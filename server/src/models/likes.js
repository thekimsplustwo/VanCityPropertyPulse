import * as data from '../data/data.js';

const { likes } = data;

const getLikes = async email => {
  //
};
const addLikes = async (email, property) => {
  //
};

const removeProperty = (email, zpid) => {
  //
};

export { getLikes, addLikes, removeProperty }
